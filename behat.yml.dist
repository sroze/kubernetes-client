default:
    suites:
        application:
            contexts:
                - ClientContext:
                    - https://baseUrl # Base server URL
                    - v1              # API version
                    - username        # Username
                    - password        # Password
                - NamespaceContext
                - SecretContext
                - ServiceContext
                - PodContext

    gherkin:
        filters:
            tags: "~@wip"
